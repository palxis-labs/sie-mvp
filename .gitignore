# Security-critical exclusions:
# - private signing keys MUST NEVER be committed
# Python
__pycache__/
*.pyc

# Virtual environments
.venv/

# SIE private keys (NEVER COMMIT)
.sie_private_key.b64
.sie_private_key.b64.pub

# Packaging artifacts
*.egg-info/

# Registry root signing key (NEVER COMMIT)
.registry_root_private_key.b64

# Local caches
.cache/
